import { Text, View } from 'react-native';

export default function CreateTicketScreen() {
  return (
    <View style={{ flex: 1, padding: 16 }}>
      <Text style={{ fontSize: 24, fontWeight: 'bold' }}>Create Ticket</Text>
      {/* TODO: Form to create a new support ticket here */}
    </View>
  );
}
