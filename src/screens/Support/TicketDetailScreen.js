import { Text, View } from 'react-native';

export default function TicketDetailScreen() {
  return (
    <View style={{ flex: 1, padding: 16 }}>
      <Text style={{ fontSize: 24, fontWeight: 'bold' }}>Ticket Details</Text>
      {/* TODO: Show ticket details and reply functionality here */}
    </View>
  );
}
